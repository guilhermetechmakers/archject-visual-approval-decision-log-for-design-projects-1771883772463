/**
 * Design tokens - single source of truth for default colors.
 * Matches CSS custom properties in index.css (--primary, --accent, etc.)
 * Use these instead of hardcoded hex values for consistency.
 */

/** Primary color (deep green) - matches --primary: 25 92 74 */
export const DEFAULT_PRIMARY_HEX = '#195C4A'

/** Accent color (mint green) - matches --accent: 123 228 149 */
export const DEFAULT_ACCENT_HEX = '#7BE495'

/** Secondary color - matches accent for branding defaults */
export const DEFAULT_SECONDARY_HEX = '#7BE495'
